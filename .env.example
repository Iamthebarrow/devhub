# =============================================================================
# DevHub Root Environment (Docker Compose)
# =============================================================================

# Django Core Settings
DJANGO_SECRET_KEY=your-secret-key-change-in-production
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Database
DATABASE_URL=postgres://devhub:devhub_local_password@postgres:5432/devhub

# CORS / CSRF
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://server:5173
CSRF_TRUSTED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://server:5173

# JWT Settings
JWT_ACCESS_LIFETIME_MINUTES=10
JWT_REFRESH_LIFETIME_DAYS=14

# Dev Admin Bootstrap (optional, for devhub_bootstrap_roles --create-admin)
DEV_ADMIN_USERNAME=admin
DEV_ADMIN_PASSWORD=changeme
DEV_ADMIN_EMAIL=admin@localhost

# Docker Access (via socket proxy by default)
DOCKER_HOST=tcp://docker-socket-proxy:2375
DOCKER_TLS_VERIFY=
DOCKER_CERT_PATH=

# Celery / Redis
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/1
CELERY_TASK_ALWAYS_EAGER=False

# Frontend (Vite)
VITE_API_BASE_URL=http://localhost:8000/api/v1
